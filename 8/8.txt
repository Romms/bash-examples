Написати скрипт, який для зазначеного доменного імені, отримає всі А записи і виведе на екран ті сервера, які недоступні.

#!/bin/bash

dnsa=$(host -t A $1 | awk '{print $4}' | tr "\n" " ")

for ip in ${dnsa[*]}
do
	ping -c2 $ip > /dev/null || echo $ip is not available
done